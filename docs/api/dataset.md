# LNMDataset

The `LNMDataset` is the central class in the `lnm-toolkit`. It manages the spatial masking and flattening of NIfTI data, ensures subject and voxel alignment across multiple data types, and provides a unified interface for downstream analyses.

::: lnm.dataset.LNMDataset
