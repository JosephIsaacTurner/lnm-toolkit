# Analysis

The `analysis` module contains the standalone mathematical functions that perform sensitivity, conjunction, and agreement mapping. These functions operate on 2D or 1D numpy arrays, avoiding direct dependency on NIfTI file formats.

::: lnm.analysis
